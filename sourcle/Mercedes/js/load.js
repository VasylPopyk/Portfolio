const load = document.querySelector('.load_polotno');
const body = document.body
window.addEventListener('DOMContentLoaded', (e) => {
    load.classList.add('loaded')
    console.log('loaded')
})